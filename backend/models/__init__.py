"""
Model registry for the backend Dash demo app.

This is intentionally lightweight and self-contained so the Dash UI can run
without depending on the heavier research pipelines.
"""

from . import registry  # re-export for `from models import registry`

